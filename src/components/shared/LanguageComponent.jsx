export const LanguageComponent = ({ tailwindStyles }) => {
    return (
        <select name="" id="language" class={tailwindStyles}>
            <option value="">English</option>
            <option value="">French</option>
        </select>
    );
};
